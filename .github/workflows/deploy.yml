name: Build and deploy Jekyll site

on:
  push:
    branches:
      - rethink-main
  pull_request:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  # Only one concurrent build per branch.
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Pages
        id: github-pages
        uses: actions/configure-pages@v5

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: '0.154.5'
          # extended: true

      - name: Build Hugo
        run: hugo --baseURL "${{ env.DEPLOYMENT_URL }}" --minify
        env:
          # DEPLOYMENT_URL is automatically set by configure-pages@v5
          DEPLOYMENT_URL: ${{ steps.github-pages.outputs.base_url }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: public/

  deploy:
    if: github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
